import { BaseModel } from '@extras/models';
import { PermissionVM, PermissionCM, PermissionUM } from '@view-models';

export class Permission extends BaseModel<PermissionVM, PermissionCM, PermissionUM> { }
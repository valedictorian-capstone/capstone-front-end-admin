import { BaseModel } from '@extras/models';
import { AuthenticationVM, AuthenticationCM, AuthenticationUM } from '@view-models';

export class Authentication extends BaseModel<AuthenticationVM, AuthenticationCM, AuthenticationUM> { }
import React from 'react';
import { RouteProps } from 'react-router-dom';
import './register.component.scss';
export const RegisterComponent = (props: Readonly<RouteProps>) => {
  return (
    <div/>
  );
};
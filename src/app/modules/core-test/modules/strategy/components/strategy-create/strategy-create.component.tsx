import React from 'react';
import { IBaseProps } from '@extras/interfaces';
import './strategy-create.component.css';

export interface IStrategyCreateComponentProps extends IBaseProps {
  input: {};
  output: {};
}

export const StrategyCreateComponent: React.FC<IStrategyCreateComponentProps> = (props: IStrategyCreateComponentProps) => {
  return <></>;
};
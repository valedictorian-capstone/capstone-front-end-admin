import { QuestionCircleOutlined } from '@ant-design/icons';
import { IBaseProps } from '@extras/interfaces';
import React, { FC, useEffect, useState } from 'react';
import './activity-type.component.css';

export interface IActivityTypeComponentProps extends IBaseProps {
    input?: {};
    output?: {};
}

export const ActivityTypeComponent: FC<IActivityTypeComponentProps> = (props: IActivityTypeComponentProps) => {
    return (
        <>
        </>
    );
};
